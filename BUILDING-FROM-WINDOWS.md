# Windows publishing, Linux building

This repository can be edited and pushed from Windows, but the target runtime and RPCS3/AppImage build remain Linux/Steam Deck focused.

See `PUSH-FROM-WINDOWS.md` for the complete publishing procedure.
